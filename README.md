Commands:

All commands must be followed by a new line (no carriage return)

a	// Retrieve the address.  Sent as two uint8s, little endian form
d   // Retrieve the data value. Sent as a single uint8
D   // Disable asserting data to the bus
D?  // Asserts the bit value of ? to the bus
